## Related Issue
Closes #32
Closes #30

## 변경 내용
- `test_fix.py`의 `reverify` 누락 mock 버그 수정 (테스트 통과 복구)
- Issue #30 Typer `check` 명령어 오작동 수정 (dummy callback 추가)
- `cli.py`: 기본 환경 점검 후 fail-fast 적용 및 HF 모델 체크 로직 추가
- `worker.py`: `--model` 입력을 위한 파이프라인(HF config/dummy 텐서 기반) 배선 및 `_train_step`/`_base_layer_device` 호환성 확보
- CLI 테스트 명령어(`["check", ...]`) 수정 및 더 이상 무효한 W9 통합 대기 테스트(`test_engine.py`) 제거

## 테스트
- [x] ruff format --check .
- [x] ruff check .
- [x] pytest
- [x] 수동 확인 (필요한 경우)

## 문서 영향
- [x] 없음 / docs 계약 갱신 / ADR 추가 / Notion 링크·상태 갱신 / Notion 기획 변경안 필요
- 이미 PR #31(contracts)에서 변경된 API 명세 준수함.

## 제한 사항 및 후속 작업
- 결과 리포트 출력(ui)에서 `skipped` 상태 렌더링은 이 PR 범위에서 제외 (이인수님 담당)
